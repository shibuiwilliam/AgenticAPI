"""AgenticAPI extensions — optional integrations installed via extras.

Each sub-package wraps a heavyweight or fast-moving dependency that
should not be required by the core framework. Install the extras to
pull in the sub-package's dependencies::

    pip install agentharnessapi[claude-agent-sdk]
"""
